<!DOCTYPE html>
<html lang="de">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Admin – Slow2Madness</title>
<style>
:root{--bg:#141417;--surface:#222228;--surface2:#2a2a32;--border:#3e3e48;
  --text:#f0ede8;--accent:#e85a42;--gold:#ddb040;--green:#51cf66;--red:#ff6b6b;}
*{box-sizing:border-box;margin:0;padding:0;}
body{background:var(--bg);color:var(--text);font-family:'Segoe UI',system-ui,sans-serif;padding:24px;}
header{display:flex;align-items:center;justify-content:space-between;margin-bottom:28px;flex-wrap:wrap;gap:12px;}
h1{font-family:Georgia,serif;color:var(--gold);font-size:1.5rem;}
.sub{color:#8a8680;font-size:.9rem;}
.btn{padding:10px 18px;border:1.5px solid var(--border);border-radius:8px;background:var(--surface2);
  color:var(--text);cursor:pointer;font-size:.85rem;transition:all .2s;}
.btn:hover{border-color:var(--accent);}
.btn-danger{border-color:rgba(255,100,100,.3);color:var(--red);}
.btn-danger:hover{border-color:var(--red);background:rgba(255,100,100,.1);}
.btn-accent{background:var(--accent);color:#fff;border:none;}
.btn-accent:hover{opacity:.9;}
.badge{padding:4px 12px;border-radius:12px;font-size:.75rem;font-weight:600;}
.badge.active{background:rgba(81,207,102,.15);color:var(--green);}
.badge.banned{background:rgba(255,100,100,.15);color:var(--red);}
.card{background:var(--surface);border:1px solid var(--border);border-radius:12px;padding:24px;margin-bottom:24px;}
.card h2{font-size:1.15rem;margin-bottom:16px;color:var(--gold);}
table{width:100%;border-collapse:collapse;font-size:.9rem;}
th{text-align:left;padding:12px;color:#8a8680;font-size:.75rem;text-transform:uppercase;letter-spacing:1px;border-bottom:1px solid var(--border);}
td{padding:14px 12px;border-bottom:1px solid var(--border);}
tr:hover{background:rgba(255,255,255,.02);}
.device-tag{display:inline-block;background:var(--surface2);padding:4px 10px;border-radius:6px;
  font-size:.75rem;margin:2px;border:1px solid var(--border);}
.device-tag button{background:none;border:none;color:var(--red);cursor:pointer;margin-left:6px;font-size:.8rem;}
input,select{padding:10px 14px;border:1.5px solid var(--border);border-radius:8px;
  background:#1a1a20;color:var(--text);font-size:.9rem;outline:none;}
input:focus,select:focus{border-color:var(--accent);}
.form-row{display:flex;gap:12px;align-items:flex-end;margin-bottom:12px;flex-wrap:wrap;}
.form-row label{font-size:.75rem;color:#8a8680;display:block;margin-bottom:6px;}
.stats{display:grid;grid-template-columns:repeat(auto-fit,minmax(150px,1fr));gap:16px;margin-bottom:24px;}
.stat-box{background:var(--surface);border:1px solid var(--border);border-radius:10px;padding:16px;text-align:center;}
.stat-box .num{font-size:1.8rem;font-weight:700;color:var(--gold);}
.stat-box .label{font-size:.8rem;color:#8a8680;margin-top:4px;}
</style>
</head>
<body>
<header>
  <div>
    <h1>⚙️ Admin Dashboard</h1>
    <div class="sub">Slow2Madness – Nutzerverwaltung</div>
  </div>
  <div style="display:flex;gap:10px;">
    <button class="btn" onclick="location.href='app.html'">Zur App</button>
    <button class="btn btn-danger" id="logoutBtn">Abmelden</button>
  </div>
</header>

<div class="stats">
  <div class="stat-box">
    <div class="num" id="totalUsers">-</div>
    <div class="label">Gesamt-Nutzer</div>
  </div>
  <div class="stat-box">
    <div class="num" id="activeUsers">-</div>
    <div class="label">Aktiv</div>
  </div>
  <div class="stat-box">
    <div class="num" id="bannedUsers">-</div>
    <div class="label">Gesperrt</div>
  </div>
</div>

<div class="card">
  <h2>➕ Neuen Nutzer erstellen</h2>
  <form id="createUserForm" class="form-row">
    <div>
      <label>Benutzername</label>
      <input type="text" id="newUsername" placeholder="Username" required>
    </div>
    <div>
      <label>Passwort</label>
      <input type="password" id="newPassword" placeholder="Passwort" required>
    </div>
    <div>
      <label>Geräte-Limit</label>
      <select id="newDevices">
        <option value="1">1 Gerät</option>
        <option value="2">2 Geräte</option>
        <option value="3">3 Geräte</option>
      </select>
    </div>
    <button type="submit" class="btn btn-accent" style="margin-bottom:0;">Erstellen</button>
  </form>
</div>

<div class="card">
  <h2>👤 Alle Nutzer</h2>
  <div style="overflow-x:auto;">
    <table>
      <thead>
        <tr>
          <th>Username</th>
          <th>Status</th>
          <th>Limit</th>
          <th>Geräte</th>
          <th>Registrierte Geräte</th>
          <th>Aktionen</th>
        </tr>
      </thead>
      <tbody id="usersTableBody">
        <tr><td colspan="6" style="text;font-weight:600;cursor:pointer;transition:opacity .2s;}
.btn:hover{opacity:.9;}
.btn:disabled{opacity:.5;cursor:not-allowed;}
.error{color:#ff6b6b;font-size:.85rem;margin-top:12px;text-align:center;display:none;}
.success{color:#51cf66;font-size:.85rem;margin-top:12px;text-align:center;display:none;}
.hint{text-align:center;color:#666;font-size:.8rem;margin-top:20px;line-height:1.5;}
</style>
</head>
<body>
<div class="login-box">
  <h1>✓ Slow2Madness</h1>
  <div class="sub">Geschützter Zugang</div>
  <form id="loginForm">
    <div class="form-group">
      <label>Benutzername</label>
      <input type="text" id="username" placeholder="Username" required autocomplete="username">
    </div>
    <div class="form-group">
      <label>Passwort</label>
      <input type="password" id="password" placeholder="Passwort" required autocomplete="current-password">
    </div>
    <button type="submit" class="btn" id="loginBtn">Anmelden</button>
    <div class="error" id="errorMsg"></div>
    <div class="success" id="successMsg"></div>
  </form>
  <div class="hint">
    Kein Account? Nur der Admin kann neue Accounts erstellen.<br>
    Kontakt: Jonas-nickel@gmx.net
  </div>
</div>

<script type="module">
import { login } from './js/auth.js';

document.getElementById('loginForm').addEventListener('submit', async (e) => {
  e.preventDefault();
  const btn = document.getElementById('loginBtn');
  const errorEl = document.getElementById('errorMsg');
  const successEl = document.getElementById('successMsg');
  
  btn.disabled = true;
  errorEl.style.display = 'none';
  successEl.style.display = 'none';
  
  const result = await login(
    document.getElementById('username').value.trim(),
    document.getElementById('password').value
  );
  
  if (result.success) {
    successEl.textContent = 'Erfolgreich! Weiterleitung...';
    successEl.style.display = 'block';
    setTimeout(() => window.location.href = result.redirect, 800);
  } else {
    errorEl.textContent = result.error;
    errorEl.style.display = 'block';
    btn.disabled = false;
  }
});
</script>
</body>
</html>
